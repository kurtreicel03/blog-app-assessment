import AuthLayout from "../layout/Auth";
import LoginForm from "../features/Login";

const LoginPage = () => {
  return (
    <AuthLayout>
      <LoginForm />
    </AuthLayout>
  );
};

export default LoginPage;
